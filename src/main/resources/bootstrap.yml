spring:
  application:
    name: bookstore-app-inter-v2
  cloud:
    config:
      profile: ${spring_cloud_config_profile}
      label: ${spring_cloud_config_label}
      uri: ${spring_cloud_config_uri}
      username: ${spring_cloud_config_username}
      password: ${spring_cloud_config_password}

#spring:
#  application:
#    name: bookstore-book-center
#  cloud:
#    config:
#      profile: dev
#      label: master
#      uri: http://config-live-dev.cread.com/
#      username: user
#      password: admin